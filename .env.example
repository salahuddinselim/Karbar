DB_HOST=127.0.0.1
DB_PORT=3306
DB_NAME=hishab
DB_USER=hishab_user
DB_PASS=change_me

# Random 32+ char secret used only to sign the CSRF-protection cookie value.
APP_SECRET=change_me_to_a_long_random_string

# Set to 1 when serving over HTTPS in production (enables secure cookies + HSTS).
APP_HTTPS=0
